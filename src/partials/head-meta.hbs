    <link rel="stylesheet" href="{{{uiRootPath}}}/css/nav.css">
    {{#if page.component}}
    <meta name="page-component" content="{{page.component.name}}">
    <meta name="page-version" content="{{page.version}}">
    <meta name="page-url" content="{{page.url}}">
	<meta name="version" content="{{page.componentVersion.displayVersion}}">
	<meta name="title" content="{{page.component.title}}">
	<meta name="context" content="{{page.componentVersion.displayVersion}} {{page.component.title}}">
    {{/if}}
